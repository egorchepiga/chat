let assert = require('chai').assert;


describe('Testing DB-module', () => {
        const db = require('./DB_module.js');
    }
);

describe('Testing CHAT-module', () => {
    const chat = require('./CHAT_module')
});